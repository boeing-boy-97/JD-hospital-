'use client';
import {useState} from 'react';import {CheckCircle2,Upload,Loader2} from 'lucide-react';
export default function JobApplicationForm({jobId,jobTitle}:{jobId:string;jobTitle:string}){const[loading,setLoading]=useState(false),[error,setError]=useState(''),[done,setDone]=useState<any>(null);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError('');const form=new FormData(e.currentTarget);form.set('job_id',jobId);try{const r=await fetch('/api/job-applications',{method:'POST',body:form});const j=await r.json();if(!r.ok)throw new Error(j.message);setDone(j)}catch(e:any){setError(e.message)}finally{setLoading(false)}}if(done)return <div className="form-card form-success"><CheckCircle2/><h2>Application received.</h2><p>Reference: <strong>{done.reference}</strong></p>{done.demo&&<p className="notice">Demo mode: connect Supabase to store the application and resume.</p>}</div>;return <form className="form-card aside-card" onSubmit={submit}><span className="eyebrow">Apply securely</span><h2 style={{fontSize:'1.7rem'}}>{jobTitle}</h2><div className="field"><label>Full name</label><input name="name" required/></div><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Phone</label><input name="phone" type="tel" required/></div><div className="field"><label>Resume</label><label className="file-input"><Upload/> PDF or DOCX, up to 5 MB<input name="resume" type="file" accept=".pdf,.docx" required/></label></div><div className="field"><label>Cover letter</label><textarea name="cover_letter" rows={4} maxLength={2000}/></div>{error&&<div className="form-error">{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?<><Loader2 className="spin"/>Uploading…</>:'Submit application'}</button></form>}
