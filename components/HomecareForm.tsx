'use client';
import {useState} from 'react';import {CheckCircle2,Loader2,AlertCircle} from 'lucide-react';
export default function HomecareForm(){const[data,setData]=useState<any>({service:'doctor-visit'});const[loading,setLoading]=useState(false);const[error,setError]=useState('');const[done,setDone]=useState<any>(null);const set=(k:string,v:string)=>setData({...data,[k]:v});async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');try{const r=await fetch('/api/homecare',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok)throw new Error(j.message);setDone(j)}catch(e:any){setError(e.message)}finally{setLoading(false)}}if(done)return <div className="form-card form-success"><CheckCircle2/><h2>Home care request received.</h2><p>Reference: <strong>{done.reference}</strong></p>{done.demo&&<p className="notice">Demo mode: connect Supabase to persist this request.</p>}</div>;return <form className="form-card" onSubmit={submit}><span className="eyebrow">Request home care</span><h2>Tell us what you need.</h2><div className="form-grid"><div className="field"><label>Service</label><select value={data.service} onChange={e=>set('service',e.target.value)}><option value="doctor-visit">Doctor visit</option><option value="nursing">Nursing</option><option value="physiotherapy">Physiotherapy</option><option value="pathology">Pathology collection</option><option value="equipment">Medical equipment</option></select></div><div className="field"><label>Location</label><input value={data.location||''} onChange={e=>set('location',e.target.value)} required/></div><div className="field"><label>Preferred date</label><input type="date" min={new Date(Date.now()+86400000).toISOString().split('T')[0]} value={data.date||''} onChange={e=>set('date',e.target.value)} required/></div><div className="field"><label>Preferred time</label><input type="time" value={data.time||''} onChange={e=>set('time',e.target.value)} required/></div><div className="field"><label>Patient name</label><input value={data.name||''} onChange={e=>set('name',e.target.value)} required/></div><div className="field"><label>Phone</label><input type="tel" value={data.phone||''} onChange={e=>set('phone',e.target.value)} required/></div><div className="field full"><label>Email</label><input type="email" value={data.email||''} onChange={e=>set('email',e.target.value)}/></div><div className="field full"><label>Additional information</label><textarea maxLength={1000} rows={3} value={data.notes||''} onChange={e=>set('notes',e.target.value)}/></div></div>{error&&<div className="form-error" role="alert"><AlertCircle/>{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?<><Loader2 className="spin"/> Sending…</>:'Request a callback'}</button></form>}
