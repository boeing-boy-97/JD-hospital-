'use client';import {useState} from 'react';import {Share2,Link as LinkIcon,Printer} from 'lucide-react';
export default function ArticleActions({title}:{title:string}){const[copied,setCopied]=useState(false);async function share(){if(navigator.share)await navigator.share({title,url:location.href});else{await navigator.clipboard.writeText(location.href);setCopied(true)}}return <div className="article-actions"><button className="btn btn-secondary" onClick={share}><Share2/> Share</button><button className="btn btn-secondary" onClick={async()=>{await navigator.clipboard.writeText(location.href);setCopied(true)}}><LinkIcon/> {copied?'Copied':'Copy link'}</button><button className="btn btn-ghost" onClick={()=>print()}><Printer/> Print</button></div>}
