'use client';
import {useEffect,useMemo,useState} from 'react';import {useRouter} from 'next/navigation';import {CalendarDays,Clock3,CheckCircle2,XCircle,LogOut,RefreshCw,Search,Bell,Menu} from 'lucide-react';import {browserSupabase} from '@/lib/supabase-browser';
type A={id:string;appointment_number:string;patient_name:string;appointment_date:string;appointment_time:string;status:string;doctors?:{name:string}|null;hospitals?:{name:string}|null};
export default function AdminDashboard({email}:{email:string}){const[list,setList]=useState<A[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[query,setQuery]=useState('');const router=useRouter();async function load(){setLoading(true);setError('');try{const r=await fetch('/api/admin/appointments');const j=await r.json();if(!r.ok)throw new Error(j.message);setList(j.appointments||[])}catch(e:any){setError(e.message)}finally{setLoading(false)}}useEffect(()=>{load()},[]);async function update(id:string,status:string){const before=list;setList(x=>x.map(a=>a.id===id?{...a,status}:a));const r=await fetch('/api/admin/appointments',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,status})});if(!r.ok){setList(before);setError('The appointment could not be updated. Please retry.')}}const today=new Date().toLocaleDateString('en-CA',{timeZone:'Asia/Kolkata'});const metrics=[['Today',list.filter(x=>x.appointment_date===today).length,CalendarDays],['Pending',list.filter(x=>x.status==='pending').length,Clock3],['Confirmed',list.filter(x=>x.status==='confirmed').length,CheckCircle2],['Completed',list.filter(x=>x.status==='completed').length,CheckCircle2],['Cancelled',list.filter(x=>x.status==='cancelled').length,XCircle]];const filtered=useMemo(()=>list.filter(x=>(x.appointment_number+x.patient_name+(x.doctors?.name||'')).toLowerCase().includes(query.toLowerCase())),[list,query]);return <div className="admin-layout"><aside className="admin-sidebar"><div className="admin-brand"><span>JD</span><b>JD Hospital<small>Operations</small></b></div><nav>{[['Overview','#dashboard'],['Appointments','#appointments'],['Public website','/']].map(([x,href],i)=><a className={i===1?'active':''} href={href} key={x}>{x}</a>)}</nav><button className="admin-user" onClick={async()=>{await browserSupabase()?.auth.signOut();router.replace('/admin/login');router.refresh()}}><span>{email[0]?.toUpperCase()}</span><b>{email}<small>Sign out</small></b><LogOut/></button></aside><main className="admin-content"><header className="admin-top"><button className="icon-button" aria-label="Open admin menu"><Menu/></button><div><span className="eyebrow">Care operations</span><h1>Appointments</h1></div><div className="admin-top-actions"><button className="icon-button" aria-label="Notifications"><Bell/></button><button className="btn btn-secondary" onClick={load}><RefreshCw/> Refresh</button></div></header><section className="admin-metrics">{metrics.map(([label,n,I]:any)=><div className="admin-metric" key={label}><I/><span>{label}</span><strong>{loading?'—':n}</strong></div>)}</section><section className="admin-table-card" id="appointments"><div className="admin-table-head"><div><h2>Upcoming appointments</h2><p>Patient information is visible only to authorised staff.</p></div><div className="input-icon"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search appointments"/></div></div>{error&&<div className="form-error">{error} <button onClick={load}>Try again</button></div>}{loading?<div className="dashboard-skeleton"><span/><span/><span/><span/></div>:filtered.length?<div className="responsive-table"><table><thead><tr><th>Appointment</th><th>Patient</th><th>Doctor / hospital</th><th>Date & time</th><th>Status</th><th>Actions</th></tr></thead><tbody>{filtered.map(a=><tr key={a.id}><td><strong>{a.appointment_number}</strong></td><td>{a.patient_name}</td><td>{a.doctors?.name||'Not assigned'}<small>{a.hospitals?.name||'Hospital pending'}</small></td><td>{a.appointment_date}<small>{String(a.appointment_time).slice(0,5)}</small></td><td><span className={`status-pill ${a.status}`}>{a.status}</span></td><td><select aria-label={`Update ${a.appointment_number} status`} value={a.status} onChange={e=>update(a.id,e.target.value)}><option value="pending">Pending</option><option value="confirmed">Confirm</option><option value="completed">Complete</option><option value="cancelled">Cancel</option></select></td></tr>)}</tbody></table></div>:<div className="empty compact"><CalendarDays/><h3>No upcoming appointments</h3><p>New appointments will appear here automatically.</p></div>}</section></main></div>}
