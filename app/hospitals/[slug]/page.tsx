import {notFound} from 'next/navigation';import Image from 'next/image';import Link from 'next/link';import type {Metadata} from 'next';import {getHospital} from '@/lib/repositories';import {Phone,MapPin,Navigation,Siren} from 'lucide-react';import HospitalMap from '@/components/HospitalMap';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const h=await getHospital((await params).slug);return h?{title:`${h.name} — ${h.city}`,description:`Location, services, doctors and appointment information for ${h.name}.`,alternates:{canonical:`/hospitals/${h.slug}`}}:{title:'Hospital not found'}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const h=await getHospital(slug);if(!h)notFound();const schema={"@context":"https://schema.org","@type":"Hospital",name:h.name,address:h.address,telephone:h.phone,geo:h.latitude&&h.longitude?{"@type":"GeoCoordinates",latitude:h.latitude,longitude:h.longitude}:undefined};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="page-hero"><div className="container"><div className="breadcrumbs">Home / Hospitals / {h.city}</div><span className="eyebrow">JD Hospital network</span><h1>{h.name}</h1><p>{h.address}</p><div className="hero-buttons"><a className="btn btn-emergency" href={'tel:'+h.emergency}><Siren size={16}/> Emergency {h.emergency}</a><Link className="btn btn-primary" href="/book-appointment">Book appointment</Link></div></div></section><Image src={h.image_url||'/images/hospital-building.jpg'} sizes="100vw" width={1600} height={1067} alt={h.image_url?`${h.name} hospital building`:'Illustrative modern healthcare building facade'} style={{width:'100%',height:'min(420px,40vw)',objectFit:'cover'}}/><section className="section"><div className="container detail-grid"><div><div className="content-block"><h2>About this hospital</h2><p>{h.name} is a demonstration multispeciality hospital designed around coordinated care, advanced clinical environments and clear patient journeys.</p></div><div className="content-block"><h2>Specialties</h2><div className="filters">{h.specialties.map(x=><span className="btn btn-secondary" key={x}>{x}</span>)}</div></div><div className="content-block"><h2>Facilities</h2><ul><li>24/7 emergency and critical care</li><li>Advanced diagnostic imaging</li><li>Operating theatres and day care</li><li>Pharmacy and laboratory services</li><li>Accessible patient and family facilities</li></ul></div><div className="content-block"><h2>Specialists at {h.city}</h2><p>Clinician availability is updated through the appointment system.</p></div>{h.latitude&&h.longitude&&<div className="content-block"><h2>Location</h2><HospitalMap lat={h.latitude} lng={h.longitude} name={h.name} address={h.address}/></div>}</div><aside><div className="aside-card"><h3>Plan your visit</h3><p><MapPin size={15}/> {h.address}</p><a className="btn btn-primary" style={{width:'100%'}} target="_blank" href={`https://www.openstreetmap.org/search?query=${encodeURIComponent(h.address)}`}><Navigation size={16}/> Get directions</a><a className="btn btn-secondary" style={{width:'100%',marginTop:8}} href={'tel:'+h.phone}><Phone size={16}/> {h.phone}</a></div></aside></div></section></>}
