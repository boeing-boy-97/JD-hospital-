'use client';
import {AlertTriangle} from 'lucide-react';export default function ErrorPage({reset}:{error:Error;reset:()=>void}){return <section className="section"><div className="container empty"><AlertTriangle/><span className="eyebrow">Something went wrong</span><h1>We couldn’t load this page.</h1><p>Your information is safe. Try again, or return to the homepage.</p><div className="hero-buttons"><button className="btn btn-primary" onClick={reset}>Try again</button><a className="btn btn-secondary" href="/">Return home</a></div></div></section>}
