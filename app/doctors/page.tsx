import type {Metadata} from 'next';import DoctorDirectory from '@/components/DoctorDirectory';import {getDoctors,getHospitals,getSpecialties} from '@/lib/repositories';
export const metadata:Metadata={title:'Find a Doctor',description:'Search JD Hospital specialists by specialty, hospital, experience, language and availability.'};
export default async function Page(){const [doctors,hospitals,specialties]=await Promise.all([getDoctors(),getHospitals(),getSpecialties()]);return <><section className="page-hero compact"><div className="container"><div className="breadcrumbs">Home / Doctors</div><span className="eyebrow">Find a doctor</span><h1>Care starts with the right specialist.</h1><p>Search by clinical need, location and language. All demonstration profiles are clearly identified.</p></div></section><section className="section directory-section"><div className="container"><DoctorDirectory doctors={doctors} hospitals={hospitals} specialties={specialties}/></div></section></>}
