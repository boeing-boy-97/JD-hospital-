'use client';import {useState} from 'react';import {Search,Loader2,ShieldCheck} from 'lucide-react';
export default function Page(){const[id,setId]=useState(''),[phone,setPhone]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState(''),[result,setResult]=useState<any>(null);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');setResult(null);try{const r=await fetch('/api/appointment-status',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({appointment_id:id.toUpperCase(),phone})});const j=await r.json();if(!r.ok)throw new Error(j.message);setResult(j)}catch(e){setError(e instanceof Error?e.message:'Unable to check status.')}finally{setLoading(false)}}return <><section className="page-hero compact"><div className="container"><span className="eyebrow">Private appointment lookup</span><h1>Check appointment status.</h1><p>Use your appointment reference and the mobile number supplied during booking.</p></div></section><section className="section section-tint"><div className="container" style={{maxWidth:700}}><form className="form-card" onSubmit={submit}><ShieldCheck className="form-hero-icon"/><div className="form-grid"><div className="field"><label>Appointment ID</label><input value={id} onChange={e=>setId(e.target.value)} placeholder="APT-2026-004821" required/></div><div className="field"><label>Mobile number</label><input type="tel" value={phone} onChange={e=>setPhone(e.target.value)} placeholder="98765 43210" required/></div></div>{error&&<div className="form-error" role="alert">{error}</div>}<button className="btn btn-primary" disabled={loading}>{loading?<><Loader2 className="spin"/>Checking…</>:<><Search/>Check status</>}</button></form>{result&&<div className="status-result" aria-live="polite"><span className={`status-pill ${result.appointment.status}`}>{result.appointment.status}</span><h2>{result.appointment.appointment_number}</h2><dl><div><dt>Doctor</dt><dd>{result.appointment.doctor}</dd></div><div><dt>Hospital</dt><dd>{result.appointment.hospital}</dd></div><div><dt>Date</dt><dd>{result.appointment.appointment_date}</dd></div><div><dt>Time</dt><dd>{result.appointment.appointment_time}</dd></div></dl>{result.demo&&<p className="notice">Demo mode: connect Supabase to retrieve persisted appointment status.</p>}</div>}</div></section></>}
