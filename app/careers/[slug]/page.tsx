import {notFound} from 'next/navigation';import {getJob} from '@/lib/repositories';import JobApplicationForm from '@/components/JobApplicationForm';
export default async function Page({params}:{params:Promise<{slug:string}>}){const job=await getJob((await params).slug);if(!job)notFound();return <><section className="page-hero compact"><div className="container"><span className="eyebrow">{job.department} · Demo role</span><h1>{job.title}</h1><p>{job.location} · {job.employment_type}</p></div></section><section className="section"><div className="container detail-grid"><article><div className="content-block"><h2>About the role</h2><p>{job.description}</p></div><div className="content-block"><h2>Responsibilities</h2><ul><li>Deliver safe, respectful and patient-centred service.</li><li>Collaborate effectively across multidisciplinary teams.</li><li>Maintain accurate records and follow hospital policies.</li></ul></div><div className="content-block"><h2>Requirements</h2><p>Requirements shown are demonstration content. Production listings must include verified qualifications and registration requirements.</p></div></article><aside><JobApplicationForm jobId={job.id} jobTitle={job.title}/></aside></div></section></>}
