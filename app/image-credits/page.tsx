import manifest from '@/data/source-manifest.json';
export default function Page(){return <><section className="page-hero compact"><div className="container"><span className="eyebrow">Content transparency</span><h1>Image credits and sources.</h1><p>Licensed editorial imagery and map sources used by this demonstration platform.</p></div></section><section className="section"><div className="container" style={{maxWidth:900}}><p>{manifest.policy}</p><div className="source-list">{manifest.sources.map(s=><article key={s.source_url+s.data_type}><span className="eyebrow">{s.data_type}</span><h2>{s.source_name}</h2><p>{s.license_or_usage_note}</p><a className="quick-link" href={s.source_url} target="_blank" rel="noreferrer">View source</a><small>Retrieved {s.retrieved_at}</small></article>)}</div></div></section></>}
