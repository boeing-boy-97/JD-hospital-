import {getPackages} from '@/lib/repositories';import PackageDirectory from '@/components/PackageDirectory';
export default async function Page(){const packages=await getPackages();return <><section className="page-hero compact"><div className="container"><div className="breadcrumbs">Home / Health packages</div><span className="eyebrow">Preventive care</span><h1>Health checks with a clear purpose.</h1><p>Compare database-managed packages by hospital and category. Demonstration prices are clearly identified.</p></div></section><section className="section directory-section"><div className="container"><PackageDirectory packages={packages}/></div></section></>}
