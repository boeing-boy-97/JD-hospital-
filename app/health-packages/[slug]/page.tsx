import {notFound} from 'next/navigation';import Link from 'next/link';import {getPackage} from '@/lib/repositories';
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=await getPackage(slug);if(!p)notFound();return <><section className="page-hero"><div className="container"><div className="breadcrumbs">Home / Health packages / {p.name}</div><span className="eyebrow">{p.category} · Demo package</span><h1>{p.name}</h1><p>{p.hospital} · {p.tests} tests · {p.duration}</p></div></section><section className="section"><div className="container detail-grid"><div><div className="content-block"><h2>Package overview</h2><p>A representative preventive screening package designed to demonstrate database-powered package content.</p></div><div className="content-block"><h2>Tests included</h2><ul><li>Complete blood count</li><li>Blood glucose profile</li><li>Lipid profile</li><li>Liver and kidney function markers</li><li>Physician consultation</li></ul></div><div className="notice">Screening packages are not a substitute for medical advice. Test selection should reflect individual clinical needs.</div></div><aside><div className="aside-card"><span className="eyebrow">Demo price</span><h2>₹{p.price.toLocaleString('en-IN')}</h2><p>Database-configured value; taxes and inclusions may vary.</p><Link className="btn btn-primary" style={{width:'100%'}} href="/book-appointment">Book package</Link></div></aside></div></section></>}
