import {createServerClient} from '@supabase/ssr';import {cookies} from 'next/headers';
export async function serverSupabase(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return null;const store=await cookies();return createServerClient(url,key,{cookies:{getAll:()=>store.getAll(),setAll:(items:{name:string;value:string;options?:Record<string,unknown>}[])=>{try{items.forEach(({name,value,options})=>store.set(name,value,options))}catch{}}}})}
export async function requireAdmin(){const db=await serverSupabase();if(!db)return {db:null,user:null,admin:null};const {data:{user}}=await db.auth.getUser();if(!user)return {db,user:null,admin:null};const {data:admin}=await db.from('admins').select('role').eq('user_id',user.id).maybeSingle();return {db,user,admin}}
